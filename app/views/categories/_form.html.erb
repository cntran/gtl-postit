<%= render 'shared/errors', obj: @category %>

<%= form_for @category, :html => { :class => 'form-inline' } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "Add Category", :class => 'btn btn-small' %>
<% end %>