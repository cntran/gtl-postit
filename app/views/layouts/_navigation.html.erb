<ul class="nav">
  <li>
    <%= link_to "Postit!", root_path, :class => 'brand' %>
  </li>
  <% if logged_in? %>
    <li><%= link_to 'New Post', new_post_path %></li>
  <% end %>
  <li>
    <div class="btn-group">
      <a class="btn btn-small dropdown-toggle" data-toggle="dropdown" href="#">
        Categories
        <span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
        <% Category.all.each do |category| %>
          <li><%= link_to category.name, category_path(category.id) %></li>
        <% end %>
        <% if logged_in? %>
        <li class="divider"></li>
        <li><%= link_to '+ Category', new_category_path %></li>
        <% end %>
      </ul>
    </div>
  </li>
  </li>
  <% if !logged_in? %>
    <li><div class="nav-item"><%= link_to 'Register', new_user_path, :class => 'btn btn-small btn-success' %></div></li>
    <li><div class="nav-item"><%= link_to 'Login', login_path, :class => 'btn btn-small' %></div></li>
  <% else %>
    <li><div class="nav-item"><%= link_to 'Logout', logout_path, :class => 'btn btn-small' %></div></li>
  <% end %>
</ul>